# 学术个人主页 · Academic Homepage

<!-- 一个简洁美观的研究生个人主页模板，支持 GitHub Pages 一键部署。 -->
Hope to update more news about me! ✨✨✨

<!-- ## ✨ 功能特性

- **关于我** — 头像、个人简介、研究方向标签、简历下载
- **最新动态** — 时间轴展示近期学术事件
- **论文发表** — 卡片式论文展示，含期刊徽章、摘要、论文/代码链接
- **关注项目** — 分类筛选，链接到各项目主页
- **联系方式** — 邮件、GitHub、Google Scholar、所在地

## 🗂 目录结构

```
academic-homepage/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── main.js
└── assets/
    ├── avatar.jpg     ← 替换为你的头像
    └── cv.pdf         ← 替换为你的简历
```

## 🚀 部署到 GitHub Pages

1. 将本项目推送到 GitHub 仓库（建议命名为 `<username>.github.io`）：

```bash
git init
git add .
git commit -m "init: academic homepage"
git branch -M main
git remote add origin https://github.com/<username>/<username>.github.io.git
git push -u origin main
```

2. 进入仓库 **Settings → Pages**，将 Source 设置为 `main` 分支 `/root` 目录，保存后等待约 1 分钟即可访问：

```
https://<username>.github.io
```

> 如果仓库名不是 `<username>.github.io`，访问地址会是：
> `https://<username>.github.io/<repo-name>/`

## 🛠 个性化修改

| 修改内容 | 文件 | 位置 |
|----------|------|------|
| 姓名、简介、单位 | `index.html` | `#about` section |
| 研究方向标签 | `index.html` | `.research-tags` |
| 论文列表 | `index.html` | `#papers` section |
| 关注项目 | `index.html` | `#projects` section |
| 联系方式 | `index.html` | `#contact` section |
| 主题色（蓝色 `#2563eb`）| `css/style.css` | `:root` 变量 `--c-accent` |
| 头像 | `assets/avatar.jpg` | 直接替换文件 |
| 简历 | `assets/cv.pdf` | 直接替换文件 | -->

<!-- ## 📄 License

MIT -->
